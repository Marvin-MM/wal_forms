[package]
name = "walrus_forms"
edition = "2024"
license = "Apache-2.0"
authors = ["WalrusForms Team"]

# Publish address — replace with the actual published address before mainnet deployment.
# During development and testing, 0x0 is used as a placeholder.
published-at = "0x0"

[dependencies]
# Sui framework is automatically included by the CLI (v1.45+).
# No explicit dependency needed.

[addresses]
walrus_forms = "0x0"
